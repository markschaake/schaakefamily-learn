import { TessellationsApp } from './_components/TessellationsApp';

export const metadata = {
  title: 'Tessellations — Schaake Family Learning',
  description: 'Discover which shapes tile cleanly without gaps.',
};

export default function TessellationsPage() {
  return <TessellationsApp />;
}
