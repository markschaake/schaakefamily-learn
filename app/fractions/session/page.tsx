import { SessionPlayer } from '../_components/session';

export const metadata = { title: 'Fractions Practice — Session' };

export default function FractionsSessionPage() {
  return <SessionPlayer />;
}
